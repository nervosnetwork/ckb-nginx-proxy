# GitHub repository path used to construct the GHCR image name.
# Default points to the upstream repo; forks should change this to their own repo.
# Example: GITHUB_REPOSITORY=myfork/ckb-nginx-proxy
GITHUB_REPOSITORY=nervosnetwork/ckb-nginx-proxy
